#include "main.h"

/**
 * _isupper - to check upper case
 * @c: int represent ASCII
 * Return: 1 if upper and 0 otherwise
*/

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
