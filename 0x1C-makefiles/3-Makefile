#!/usr/bin/make

CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school
RM = rm -f

all: ${NAME}

clean:
	${RM} *~ ${NAME}

oclean:
	${RM} main.o ${NAME}

fclean: clean oclean

re: clean all

${NAME}: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

%.o:%.c
	${CC} -c -o $@ $<
