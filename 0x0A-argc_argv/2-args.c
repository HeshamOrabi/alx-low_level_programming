#include <stdio.h>

/**
 * main - print all arguments passed into the File
 * @argc: Number of command-line arguments
 * @argv: Array of command-line arguments
 *
 * Return: 0 if successful
*/
int main(int argc, char *argv[])
{
	int i;

	if (i = 0; i < argc; i++)
	{	
		printf("%s\n", argv[i]);
	}

	return (0);
}
